import Teamy from '../src/index';

export default Teamy;

export const {
    TeamsManager,
    Team,
    ParentTeam,
    SubTeam,
    TeamsHandler,
    PointsHandler,
    SubPointsHandler,
    ParentPointsHandler,
    GuildMemberHandler,
    TeamMembersHandler,
} = Teamy;